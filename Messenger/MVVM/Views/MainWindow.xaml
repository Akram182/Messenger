<Window x:Class="Messenger.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Messenger"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Button Width="150" Height="50" VerticalAlignment="Top" Margin="0,10" Click="Button_Click"/>

        <ListView Name="UserList" BorderBrush="#cdcdcd" Margin="10,70,10,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Login}" Header="Login"/>
                    <GridViewColumn DisplayMemberBinding="{Binding UserName}" Header="Naem"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Password}" Header="Passwored"/>
                </GridView>
            </ListView.View>

        </ListView>
    </Grid>
</Window>
